(function () {
  'use strict';

  /* @ngdoc object
   * @name app.components
   * @description
   * Module for the components (reusable) portions of the application
   */
  angular
    .module('app.components', ['app.core']);
}());
