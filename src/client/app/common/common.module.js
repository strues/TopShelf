(function () {
  'use strict';

  /* @ngdoc object
   * @name app.common
   * @description
   * Module for the common (shared) portions of the application
   */
  angular
    .module('app.common', []);
}());
