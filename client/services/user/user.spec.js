'use strict';

describe('Service: user', function () {

  beforeEach(module('app'));

  var user;
  beforeEach(inject(function (_user_) {
    user = _user_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
