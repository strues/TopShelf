'use strict';

describe('Service: modal', function () {

  beforeEach(module('app'));

  var modal;
  beforeEach(inject(function (_modal_) {
    modal = _modal_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
